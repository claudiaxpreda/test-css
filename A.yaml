# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool: Default


pr:
  branches:
    include:
    - main
    - test1
    - test2
  paths:
    include:
    - test-css/testA
    - test-css/testB
    exclude:
    - test-css/*
    - test-css/testC

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - script: echo Building from A!
